# Grid regression coverage matrix

The canonical coverage matrix is now the self-contained HTML page:

- [`coverage-matrix.html`](coverage-matrix.html)

Open it locally in a browser for the intended readable table view:

```bash
open docs/implementation/testing/coverage-matrix.html
```

The HTML view uses full column names (`Frontend`, `Backend`, `Client`, `Infinite`, `SSRM`), colored coverage badges, sticky table headings and a capability search box.

This Markdown file remains only as a compatibility pointer for older repository links/bookmarks that referenced `coverage-matrix.md`. Do not maintain a second copy of the matrix here.
