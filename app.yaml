command:
  - gunicorn
  - --chdir
  - backend
  - config.wsgi:application
  - --bind
  - 0.0.0.0:${DATABRICKS_APP_PORT}
  - --workers
  - '2'
env:
  - name: DJANGO_DEBUG
    value: 'false'
  - name: DJANGO_ALLOWED_HOSTS
    value: '*'
